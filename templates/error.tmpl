{{template "base" .}}

{{define "title"}}
	Error {{.Status}}: {{.StatusText}}
{{end}}

{{define "main"}}
	<h2>Error {{.Status}}: {{.StatusText}}</h2>

	<p>{{.Description}}</p>

	{{if .Cause}}
		<p><code>{{.Cause}}</code></p>
	{{end}}
{{end}}
